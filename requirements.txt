# ============================================================
# AI Resume Parsing Pipeline — Local/Air-Gapped Dependencies
# ============================================================
# Install: pip install -r requirements.txt
# GPU Note: Replace `paddlepaddle` with `paddlepaddle-gpu`
#           if you have a CUDA-capable GPU.
# ============================================================

# --- Core ---
pydantic>=2.0,<3.0
pymupdf>=1.24.0

# --- Phase 2: Document Parsing ---
docling>=2.0.0
paddlepaddle>=2.6.0          # CPU version; use paddlepaddle-gpu for CUDA
paddleocr>=2.7.0
python-docx>=1.1.0
Pillow>=10.0.0

# --- Phase 3: Local LLM Extraction ---
openai>=1.0.0                # OpenAI-compatible client for ollama
instructor>=1.0.0            # Structured output enforcement
ollama>=0.3.0                # Ollama Python SDK

# --- Phase 4: Embeddings & Matching ---
sentence-transformers>=3.0.0
FlagEmbedding>=1.2.0
torch>=2.0.0

# --- Phase 5: Benchmarking & Utilities ---
tabulate>=0.9.0
rich>=13.0.0
